name: Deploy

on:
  workflow_dispatch:
    inputs:
      environment:
        type: choice
        options: [dev, stage, prod]
      image_tag:
        required: true

jobs:
  deploy:
    runs-on: ubuntu-latest   # 👈 ESTO FALTABA
    environment: ${{ inputs.environment }}

    steps:
      - name: Deploy
        run: |
          echo "Deploying backend:${{ inputs.image_tag }} to ${{ inputs.environment }}"
